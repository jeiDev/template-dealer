document.body.onload = init

function init(){

    // DECLARE VARIABLES
    let create = new createDOM("../../") // DECLARED CLASS CREATE
    
     // Create header
     create.header()
 
     // Create footer
     create.footer()
}